# XNav Vision Core Python Dependencies
opencv-python-headless>=4.8.0
numpy>=1.24.0
# dt-apriltags is a maintained fork of pupil-apriltags with ARM64 (aarch64) wheels.
# The code imports it as "apriltag" via: import dt_apriltags as apriltag
dt-apriltags>=3.0.0
pyntcore>=2024.0.0
flask>=3.0.0
flask-socketio>=5.3.0
RPi.GPIO>=0.7.1
