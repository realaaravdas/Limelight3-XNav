# XNav Vision Core Python Dependencies
# NOTE: The ISO build uses the C++ binary — these are for Python development only.
opencv-python-headless>=4.8.0
numpy>=1.24.0
# robotpy-apriltag replaces dt-apriltags (which had no arm64/manylinux wheel and
# required pupil-apriltags>=1.0.4 as a transitive dependency — unavailable on arm64).
# robotpy-apriltag ships prebuilt linux_aarch64 wheels and has no extra dependencies.
robotpy-apriltag>=2024.1.0
pyntcore>=2024.0.0
flask>=3.0.0
flask-socketio>=5.3.0
RPi.GPIO>=0.7.1
