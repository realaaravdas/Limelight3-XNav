# xnav-dashboard.service is no longer needed.
# The web dashboard (port 5800) is now built into the C++ xnav binary
# and served by xnav-vision.service.
#
# This file is kept for backwards compatibility but should NOT be enabled.
# All dashboard functionality is served by /opt/xnav/bin/xnav on port 5800.
